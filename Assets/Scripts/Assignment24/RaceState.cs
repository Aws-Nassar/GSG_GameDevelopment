namespace Assignment24
{
    //Create a new C# script named RaceState.
    public enum RaceState
    {
        //In it define an Enum called RaceState with the following values: Start, Accelerate, Turn, Crash, Finish
        Start,
        Accelerate,
        Turn,
        Crash,
        Finish,
    }
}

